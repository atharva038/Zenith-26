# 🔄 Backend API Flow Diagram

## Authentication Flow

```
┌─────────────┐
│   Frontend  │
│ Admin Login │
└──────┬──────┘
       │
       │ POST /api/auth/login
       │ { email, password }
       ▼
┌─────────────────────┐
│  Auth Controller    │
│  - Validate input   │
└──────┬──────────────┘
       │
       │ Call auth.service.login()
       ▼
┌─────────────────────┐
│   Auth Service      │
│  - Find user        │
│  - Verify password  │
│  - Generate JWT     │
└──────┬──────────────┘
       │
       │ Return { user, token }
       ▼
┌─────────────────────┐
│   MongoDB           │
│   User Collection   │
└─────────────────────┘
       │
       │ User found & verified
       ▼
┌─────────────────────┐
│   Frontend          │
│ Store token         │
│ localStorage/cookie │
└─────────────────────┘
```

## Media Upload Flow

```
┌─────────────────┐
│    Frontend     │
│  Admin Panel    │
│  Upload Form    │
└────────┬────────┘
         │
         │ POST /api/media/upload
         │ Authorization: Bearer {token}
         │ FormData: { file, title, description, ... }
         ▼
┌─────────────────────────┐
│   Auth Middleware       │
│  - Verify JWT token     │
│  - Check admin role     │
└────────┬────────────────┘
         │ Token valid ✓
         ▼
┌─────────────────────────┐
│   Upload Middleware     │
│  - Multer file parsing  │
│  - Validate file type   │
│  - Check file size      │
└────────┬────────────────┘
         │ File valid ✓
         ▼
┌─────────────────────────┐
│  Media Controller       │
│  - Extract metadata     │
│  - Call media service   │
└────────┬────────────────┘
         │
         │ Call uploadMedia()
         ▼
┌─────────────────────────┐
│   Media Service         │
│  - Prepare file data    │
│  - Call Cloudinary      │
└────────┬────────────────┘
         │
         │ Upload file buffer
         ▼
┌─────────────────────────┐
│  Cloudinary Service     │
│  - Stream upload        │
│  - Get transformed URL  │
└────────┬────────────────┘
         │
         │ Upload to cloud
         ▼
┌─────────────────────────┐
│    CLOUDINARY CDN       │
│  ☁️  File stored        │
│  Returns: { url,        │
│    publicId, size,      │
│    width, height, ... } │
└────────┬────────────────┘
         │
         │ Return upload result
         ▼
┌─────────────────────────┐
│   Media Service         │
│  - Create Media doc     │
│  - Save to MongoDB      │
└────────┬────────────────┘
         │
         │ Save metadata
         ▼
┌─────────────────────────┐
│      MongoDB            │
│   Media Collection      │
│  {                      │
│    title, url,          │
│    secureUrl,           │
│    cloudinaryId,        │
│    uploadedBy, ...      │
│  }                      │
└────────┬────────────────┘
         │
         │ Document saved
         ▼
┌─────────────────────────┐
│     Frontend            │
│  Show success message   │
│  Display uploaded media │
└─────────────────────────┘
```

## Media Retrieval Flow (Frontend Glimpses Page)

```
┌─────────────────┐
│   Frontend      │
│ Glimpses Page   │
└────────┬────────┘
         │
         │ GET /api/media?type=image&isActive=true
         │ No auth required (public endpoint)
         ▼
┌─────────────────────────┐
│   Media Controller      │
│  - Parse query params   │
│  - Call media service   │
└────────┬────────────────┘
         │
         │ Call getAllMedia()
         ▼
┌─────────────────────────┐
│   Media Service         │
│  - Build query filters  │
│  - Query MongoDB        │
│  - Apply pagination     │
└────────┬────────────────┘
         │
         │ Find media
         ▼
┌─────────────────────────┐
│      MongoDB            │
│   Media Collection      │
│  Query: {               │
│    type: 'image',       │
│    isActive: true       │
│  }                      │
└────────┬────────────────┘
         │
         │ Return documents
         ▼
┌─────────────────────────┐
│   Media Service         │
│  Return {               │
│    media: [...],        │
│    pagination: {        │
│      total, page, ...   │
│    }                    │
│  }                      │
└────────┬────────────────┘
         │
         │ Send response
         ▼
┌─────────────────────────┐
│     Frontend            │
│  Render images using:   │
│  media[i].secureUrl     │
│  (Cloudinary URLs)      │
└─────────────────────────┘
         │
         │ Load images
         ▼
┌─────────────────────────┐
│    CLOUDINARY CDN       │
│  ☁️  Serve optimized    │
│     images via CDN      │
│  (Fast, cached)         │
└─────────────────────────┘
```

## Media Delete Flow

```
┌─────────────────┐
│    Frontend     │
│  Admin Panel    │
│  Delete Button  │
└────────┬────────┘
         │
         │ DELETE /api/media/:id
         │ Authorization: Bearer {token}
         ▼
┌─────────────────────────┐
│   Auth Middleware       │
│  - Verify JWT token     │
│  - Check admin role     │
└────────┬────────────────┘
         │ Token valid ✓
         ▼
┌─────────────────────────┐
│  Media Controller       │
│  - Get media ID         │
│  - Call media service   │
└────────┬────────────────┘
         │
         │ Call deleteMedia(id)
         ▼
┌─────────────────────────┐
│   Media Service         │
│  - Find media doc       │
│  - Get publicId         │
└────────┬────────────────┘
         │
         │ Query by ID
         ▼
┌─────────────────────────┐
│      MongoDB            │
│   Media Collection      │
│  Find document          │
└────────┬────────────────┘
         │
         │ Document found
         ▼
┌─────────────────────────┐
│   Media Service         │
│  - Call Cloudinary      │
│  - Delete from cloud    │
└────────┬────────────────┘
         │
         │ Delete by publicId
         ▼
┌─────────────────────────┐
│  Cloudinary Service     │
│  - Call destroy()       │
└────────┬────────────────┘
         │
         │ Delete from cloud
         ▼
┌─────────────────────────┐
│    CLOUDINARY CDN       │
│  ☁️  File deleted       │
└────────┬────────────────┘
         │
         │ Deletion confirmed
         ▼
┌─────────────────────────┐
│   Media Service         │
│  - Delete from MongoDB  │
└────────┬────────────────┘
         │
         │ Delete document
         ▼
┌─────────────────────────┐
│      MongoDB            │
│   Media Collection      │
│  Document deleted       │
└────────┬────────────────┘
         │
         │ Success
         ▼
┌─────────────────────────┐
│     Frontend            │
│  Show success message   │
│  Remove from UI         │
└─────────────────────────┘
```

## Request/Response Examples

### Login Request/Response

```javascript
// Request
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "admin@zenith2026.com",
  "password": "Admin@2026"
}

// Response (Success)
{
  "success": true,
  "message": "Login successful",
  "data": {
    "user": {
      "id": "507f1f77bcf86cd799439011",
      "username": "admin",
      "email": "admin@zenith2026.com",
      "role": "admin",
      "lastLogin": "2025-11-22T10:30:00.000Z"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### Upload Request/Response

```javascript
// Request
POST http://localhost:5000/api/media/upload
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: multipart/form-data

FormData:
  file: [image.jpg]
  title: "Opening Ceremony"
  description: "Zenith 2026 opening ceremony highlights"
  category: "event"
  tags: "ceremony,2026,opening"

// Response (Success)
{
  "success": true,
  "message": "Media uploaded successfully",
  "data": {
    "media": {
      "_id": "507f1f77bcf86cd799439012",
      "title": "Opening Ceremony",
      "description": "Zenith 2026 opening ceremony highlights",
      "type": "image",
      "url": "http://res.cloudinary.com/demo/image/upload/v1234567890/zenith2026/glimpses/images/abc123.jpg",
      "secureUrl": "https://res.cloudinary.com/demo/image/upload/v1234567890/zenith2026/glimpses/images/abc123.jpg",
      "publicId": "zenith2026/glimpses/images/abc123",
      "format": "jpg",
      "size": 2048576,
      "width": 1920,
      "height": 1080,
      "tags": ["ceremony", "2026", "opening"],
      "category": "event",
      "isActive": true,
      "uploadedBy": "507f1f77bcf86cd799439011",
      "createdAt": "2025-11-22T10:35:00.000Z",
      "updatedAt": "2025-11-22T10:35:00.000Z"
    }
  }
}
```

### Get All Media Request/Response

```javascript
// Request
GET http://localhost:5000/api/media?type=image&page=1&limit=10

// Response (Success)
{
  "success": true,
  "message": "Media retrieved successfully",
  "data": {
    "media": [
      {
        "_id": "507f1f77bcf86cd799439012",
        "title": "Opening Ceremony",
        "secureUrl": "https://res.cloudinary.com/...",
        "thumbnail": "https://res.cloudinary.com/...",
        "type": "image",
        "category": "event",
        "createdAt": "2025-11-22T10:35:00.000Z"
      },
      // ... more media items
    ],
    "pagination": {
      "total": 45,
      "page": 1,
      "pages": 5,
      "limit": 10
    }
  }
}
```

## Middleware Chain Example

```
Request → Express App
           ↓
       [CORS] → Allow frontend origin
           ↓
       [Helmet] → Set security headers
           ↓
       [Rate Limit] → Check request limit
           ↓
       [Body Parser] → Parse JSON/Form data
           ↓
       [Cookie Parser] → Parse cookies
           ↓
       [Morgan] → Log request
           ↓
       [Route Handler] → /api/media/upload
           ↓
       [Auth Middleware] → Verify JWT
           ↓
       [Admin Middleware] → Check admin role
           ↓
       [Upload Middleware] → Parse file with Multer
           ↓
       [Validate Middleware] → Check input
           ↓
       [Controller] → Process request
           ↓
       [Service] → Business logic
           ↓
       [Model] → Database operation
           ↓
       [Response] → Send JSON response
           ↓
       [Error Handler] → Catch any errors
           ↓
       Response → Frontend
```

## Error Handling Flow

```
┌─────────────────┐
│  Any Route      │
└────────┬────────┘
         │
         │ Error occurs
         ▼
┌─────────────────────────┐
│   Try-Catch Block       │
│   or Promise.catch()    │
└────────┬────────────────┘
         │
         │ throw error
         ▼
┌─────────────────────────┐
│  Error Handler          │
│  Middleware             │
│  (errorHandler.js)      │
└────────┬────────────────┘
         │
         │ Identify error type
         ▼
┌─────────────────────────┐
│  Format Error:          │
│  - Mongoose errors      │
│  - Validation errors    │
│  - JWT errors           │
│  - Custom errors        │
└────────┬────────────────┘
         │
         │ Send formatted error
         ▼
┌─────────────────────────┐
│  Response:              │
│  {                      │
│    success: false,      │
│    message: "Error...", │
│    statusCode: 400/500  │
│  }                      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│     Frontend            │
│  Display error message  │
└─────────────────────────┘
```

## Database Relationships

```
┌─────────────────────────┐
│   User Collection       │
│  {                      │
│    _id: ObjectId        │◄───────┐
│    username: String     │        │
│    email: String        │        │
│    password: String     │        │
│    role: "admin"        │        │
│  }                      │        │
└─────────────────────────┘        │
                                   │
                                   │ uploadedBy
                                   │ (Reference)
┌─────────────────────────┐        │
│   Media Collection      │        │
│  {                      │        │
│    _id: ObjectId        │        │
│    title: String        │        │
│    secureUrl: String    │        │
│    cloudinaryId: String │        │
│    uploadedBy: ObjectId │────────┘
│    category: String     │
│    tags: [String]       │
│    isActive: Boolean    │
│  }                      │
└─────────────────────────┘
```

## Tech Stack Visualization

```
┌─────────────────────────────────────────┐
│          FRONTEND (Future)              │
│     React/Next.js Admin Panel           │
│  - Login Form                           │
│  - Upload Interface                     │
│  - Gallery View                         │
└────────────┬────────────────────────────┘
             │
             │ HTTP/HTTPS
             │ REST API Calls
             ▼
┌─────────────────────────────────────────┐
│         EXPRESS.JS SERVER               │
│  ┌───────────────────────────────────┐  │
│  │  Middleware Layer                 │  │
│  │  - CORS, Helmet, Rate Limit       │  │
│  │  - Auth, Validation, Upload       │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Routes Layer                     │  │
│  │  - /api/auth/*                    │  │
│  │  - /api/media/*                   │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Controllers Layer                │  │
│  │  - Request handling               │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Services Layer                   │  │
│  │  - Business logic                 │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Models Layer                     │  │
│  │  - Mongoose schemas               │  │
│  └───────────────────────────────────┘  │
└───────┬──────────────────────┬──────────┘
        │                      │
        │                      │
        ▼                      ▼
┌──────────────────┐  ┌──────────────────┐
│    MongoDB       │  │   Cloudinary     │
│  ☁️ Database     │  │  ☁️ CDN Storage  │
│  - User data     │  │  - Images        │
│  - Media refs    │  │  - Videos        │
└──────────────────┘  └──────────────────┘
```

---

This visual guide helps understand how all the components work together!

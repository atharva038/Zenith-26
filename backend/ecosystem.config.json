{
    "name": "zenith-backend",
    "version": "1.0.0",
    "scripts": [
        {
            "name": "zenith-backend",
            "script": "server.js",
            "instances": 1,
            "exec_mode": "cluster",
            "env": {
                "NODE_ENV": "production"
            }
        }
    ]
}